spring:
  application:
    name: micro-service-netflix-server
  profiles:
    active: dev,test,prod
  cloud:
    config:
#      uri: ${SPRING_CONFIG_URI:<config server domain name>}
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}

---

concurrent:
  quantity: 5